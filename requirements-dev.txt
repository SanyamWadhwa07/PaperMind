# Dev, test and eval dependencies — on top of requirements.txt and
# backend/requirements.txt:
#
#   pip install -r requirements.txt -r backend/requirements.txt -r requirements-dev.txt

# ── Testing ──────────────────────────────────────────────────────────────────
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-mock>=3.11.0
pytest-cov>=4.1.0

# ── Benchmarking (evals/run_benchmark.py) ────────────────────────────────────
rouge-score>=0.1.2        # summary quality vs. each paper's own abstract

# ── Code quality ─────────────────────────────────────────────────────────────
black
flake8
mypy
