# ── Core web framework ──────────────────────────────────────────────────────
fastapi
uvicorn[standard]
python-multipart          # file upload support for FastAPI
werkzeug                  # secure_filename utility

# ── Auth ────────────────────────────────────────────────────────────────────
python-dotenv
PyJWT
bcrypt

# ── Database ─────────────────────────────────────────────────────────────────
supabase
pgvector

# ── PDF extraction ───────────────────────────────────────────────────────────
PyMuPDF                   # fitz — PDF parsing, figure extraction
pymupdf4llm               # LLM-optimised Markdown extraction from PDFs
pdfplumber                # table extraction fallback

# ── LLM backends ─────────────────────────────────────────────────────────────
ollama                    # local inference via Ollama (default)
transformers              # HuggingFace transformers fallback
torch                     # required by transformers
# Optional cloud LLM backends (uncomment or install as needed):
# openai                  # PAPERMIND_LLM_BACKEND=openai  OPENAI_API_KEY=...
# anthropic               # PAPERMIND_LLM_BACKEND=anthropic  ANTHROPIC_API_KEY=...

# ── NLP & embeddings ─────────────────────────────────────────────────────────
sentence-transformers     # all-MiniLM-L6-v2 for paper embeddings
nltk
PyYAML
arxiv
requests

# ── Rate limiting ─────────────────────────────────────────────────────────────

# ── Cache & rate limiting ────────────────────────────────────────────────────
redis                     # rate-limit buckets + PDF processing cache; both
                          #   degrade to in-memory when Redis is unreachable

# ── Logging & monitoring ─────────────────────────────────────────────────────
structlog
python-json-logger
sentry-sdk[fastapi]       # optional: set SENTRY_DSN env var to enable

# ── HTTP client ──────────────────────────────────────────────────────────────
httpx                     # external API calls + FastAPI TestClient transport

# Test and lint tooling lives in requirements-dev.txt, not here — a production
# image has no business installing black, flake8 and mypy. `arq` was listed
# here for a task queue that was never implemented; the queue that now exists
# (backend/services/job_queue.py) is a plain asyncio worker over a Postgres
# table — see backend/database/migrations/012_processing_jobs.sql — so it adds
# no dependency here at all.
